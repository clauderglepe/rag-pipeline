// src/ingestion/chunking.service.ts
// Función(es) pura(s), sin dependencias de Nest — así se testea sin levantar el módulo.
// Implementación real: tarea "feat(ingestion): add fixed-size chunking with overlap"
export {};
// src/query/dto/query-request.dto.ts
// Validación manual — mismo criterio que las Fases 2, 3 y 4 (ADR-0001 §6: la
// decisión de librería de validación de DTOs se toma recién en la Fase 6).
// Implementación real: tarea "feat(query): add rag query endpoint"
export {};